# The LinkedIn export stays local. It is source material, not something to publish.
*.pdf

.DS_Store
Thumbs.db
node_modules/

# The LinkedIn export is source material, not something to publish.
data/

# Raw CAD/OBJ archives. The converted, compressed GLBs live in assets/models/.
3d models/
